import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic',
  appName: 'Ionic-UAS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
